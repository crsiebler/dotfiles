Use the existing package manager and pinned SDK; inspect its registration/schema interfaces before choosing imports. Expose support_search_tickets(query, cursor?, limit?) and support_get_ticket(id), with bounded text/limit and ID validation. Return ticket summaries with opaque next_cursor and explicit truncation; fetch details separately. Keep tools read-only and open-world, with no mutation capability. Separate provider mapping, schema/handlers and stdio entry point. Send redacted diagnostics to stderr only. Fake provider tests cover a second page, empty data, invalid ID, 401, 429 and timeout. Then a real SDK client initializes the compiled entry, lists tools, calls fixture success/failure cases, and closes. Check manifest bin against emitted nested output and launch from another cwd. No live API calls, installs, or implementation are performed in this planning response.
