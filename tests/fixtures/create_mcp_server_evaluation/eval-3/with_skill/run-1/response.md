This is an existing-service connection request, not a create-mcp-server trigger. Use the client's remote MCP configuration documentation and the vendor's current authentication/scopes documentation. Do not scaffold a wrapper, copy an SDK example, expose the token, or assume authorization to alter global configuration. Prepare the requested connection configuration with secret references and identify account-side prerequisites; configure only within the user's authorized destination.
